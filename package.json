{
  "name": "@caffeine/template-app",
  "type": "module",
  "engines": {
    "node": ">=16.0.0",
    "pnpm": ">=7.0.0",
    "npm": "please use pnpm"
  },
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "typecheck": "pnpm -r --if-present run typecheck",
    "check": "pnpm -r --if-present run check",
    "fix": "pnpm -r --if-present run fix",
    "bindgen": "caffeine-bindgen --did-file ./src/backend/dist/backend.did --out-dir ./src/frontend/src --actor-interface-file --force",
    "setup": "mops install && pnpm install",
    "local:start": "dfx start --background --host 127.0.0.1:4943",
    "local:deploy": "dfx deploy",
    "local:bindgen": "pnpm bindgen",
    "local:dev": "pnpm --filter frontend dev"
  },
  "devDependencies": {
    "sharp": "^0.34.4"
  }
}
